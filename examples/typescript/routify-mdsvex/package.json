{
  "name": "svite-example-routify-mdsvex-typescript",
  "version": "0.0.0",
  "scripts": {
    "validate": "svelte-check && tsc --noEmit",
    "dev": "run-p dev:routify dev:svite",
    "dev:routify": "routify run",
    "dev:svite": "svite -ts",
    "build": "run-s build:routify build:svite",
    "build:routify": "routify run -b",
    "prebuild:svite": "npm run validate",
    "build:svite": "svite build -ts"
  },
  "dependencies": {
    "svelte": "^3.24.0",
    "@sveltech/routify": "^1.9.5"
  },
  "devDependencies": {
    "@tsconfig/svelte": "^1.0.0",
    "mdsvex": "^0.8.2",
    "npm-run-all": "^4.1.5",
    "svelte-check": "^0.1.0",
    "svite": "latest",
    "tslib": "^2.0.0",
    "typescript": "^3.9.7",
    "vite": "1.0.0-beta.12"
  }
}
