{
  "name": "svite-example-routify-mdsvex",
  "version": "0.0.0",
  "scripts": {
    "dev": "run-p dev:routify dev:svite",
    "dev:routify": "routify run",
    "dev:svite": "svite",
    "build": "run-s build:routify build:svite",
    "build:routify": "routify run -b",
    "build:svite": "svite build"
  },
  "dependencies": {
    "svelte": "^3.24.0",
    "@sveltech/routify": "^1.9.5"
  },
  "devDependencies": {
    "mdsvex": "^0.8.2",
    "npm-run-all": "^4.1.5",
    "svite": "latest",
    "vite": "1.0.0-rc.4"
  }
}
