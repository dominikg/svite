# build and test on linux, windows, mac with node 12,14
name: Test

on:
  push:
    branches:
      - master
      - beta
  pull_request:
    branches:
      - master
      - beta

jobs:
  test:
    env:
      CI: true
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest,windows-latest,macos-latest]
        node-version: [12.x, 14.x]
        testNamePattern1: ['javascript','typescript'],
        testNamePattern2: ['npm','pnpm','yarn','yarn2']
        include:
          - testNamePattern1: 'svite'
            testNamePattern2: 'hmr-test'
    steps:
      - uses: actions/checkout@v2
      - name: Cache node modules
        uses: actions/cache@v2
        env:
          cache-name: cache-test-dependencies-${{ matrix.node-version }}
          testNamePattern: ${{ matrix.testNamePattern1 }} ${{ matrix.testNamePattern2 }}
        with:
          path: ./test/cache
          key: ${{ runner.os }}-test-${{ env.cache-name }}-${{ hashFiles('test/temp/**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-test-${{ env.cache-name }}-
            ${{ runner.os }}-test-
            ${{ runner.os }}-
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci
      - run: npm run build --if-present
      - run: npm i -g pnpm
      - run: echo "testing ${{env.testNamePattern}}"
      - run: npm test  --scripts-prepend-node-path -- --testNamePattern="${{env.testNamePattern}}"
      - name: archive test/temp directory
        if: failure()
        shell: bash
        run: tar -cvf test-temp.tar --exclude="node_modules" --exclude=".yarn" --exclude="dist" --exclude=".pnp.js" test/temp/
      - uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: test-temp-${{ matrix.os }}-${{ matrix.node-version }}
          path: |
            test-temp.tar
            test/temp/jest-results.json
            test/temp/junit.xml
      - uses: actions/upload-artifact@v2
        if: success()
        with:
          name: test-results-${{ matrix.os }}-${{ matrix.node-version }}
          path: |
            test/temp/jest-results.json
            test/temp/junit.xml
